# Pen Fight — Supabase project "penfight" (ap-south-1)
#
# These two values are meant to be public: the publishable key is shipped in
# every browser bundle by design, and every table is locked behind RLS with
# writes only reachable through SECURITY DEFINER functions. Committing them
# keeps the repo runnable straight after a clone.
VITE_SUPABASE_URL=https://pjndmhljzuoxgdnosiea.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_ZHshIBKCMSFZiznQqprydQ_C_yKW57I
